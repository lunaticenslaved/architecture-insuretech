http {
    server_tokens off;
    
    # Зона для Rate Limiting: ключ — IP клиента, лимит 10 запросов в минуту.
    # rate=10r/m означает не более 10 запросов в минуту с одного $binary_remote_addr.
    limit_req_zone $binary_remote_addr zone=partner_limit:10m rate=10r/m;

    # При превышении лимита возвращаем HTTP 429 (Too Many Requests).
    limit_req_status 429;

    # Настройка upstream для балансировки нагрузки
    upstream backend_servers {
        server backend1.example.com;
        server backend2.example.com;
        server backend3.example.com;
    }

    server {
        listen 80;

        location / {
            limit_req zone=partner_limit;

            proxy_pass http://backend_servers;
        }

    }
}
